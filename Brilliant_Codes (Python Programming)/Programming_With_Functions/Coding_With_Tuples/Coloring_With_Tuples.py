for pixel in image:
    pixel = only_red(pixel)
def only_red(color):
    (r, g, b) = color
    return (r, 0, 0)

"A color can be represented as a tuple, which groups related values in code."

# Wrire the only_green functions to return a new color that's purely green.

for pixel in image:
    pixel = only_green(pixel)
    def only_green(color):
    (r, g, b) = color
    return (0, g, 0)

"Grouped data structures like tuples or lists are commonly used as function inputs and output."


